name: Update AdGuard Rules

on:
  schedule:
    # UTC时间凌晨2点运行，即北京时间上午10点
    - cron: '0 2 * * *'
  workflow_dispatch:
  push:
    paths:
      - 'sources/sources.txt'
      - 'scripts/**'

jobs:
  update-rules:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Set Beijing Timezone
      uses: szenius/set-timezone@v1.2
      with:
        timezoneLinux: "Asia/Shanghai"

    # 1. 检出主分支（包含脚本）到默认工作区
    - name: Checkout main branch (scripts)
      uses: actions/checkout@v4
      with:
        ref: main

    # 2. 检出 rules 分支到子目录，用于存放最终规则
    - name: Checkout rules branch (for output)
      uses: actions/checkout@v4
      with:
        ref: rules
        path: rules-output

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    # 3. 在工作区根目录运行脚本，生成规则文件
    - name: Generate rules
      run: |
        echo "开始更新规则..."
        python scripts/fetch_rules.py
        python scripts/merge_rules.py
        python scripts/optimize_rules.py
        echo "所有规则生成完成。"

    # 4. 在 rules-output 目录中整理并归档文件
    - name: Prepare and Organize Rules
      run: |
        cd ./rules-output
        
        # 设置日期变量
        TODAY=$(date +'%Y-%m-%d')
        
        # 为今天的规则在 archive 目录下创建日期归档
        mkdir -p "./archive/$TODAY/full"
        mkdir -p "./archive/$TODAY/lite"
        
        # 从工作区根目录复制新生成的文件到归档目录
        cp ../rules/merged_all.txt "./archive/$TODAY/full/"
        cp ../rules/merged_lite.txt "./archive/$TODAY/lite/"
        cp ../rules/merge_stats.txt "./archive/$TODAY/full/" 2>/dev/null || true
        cp ../rules/optimization_stats.txt "./archive/$TODAY/lite/" 2>/dev/null || true
        
        # === 自动清理旧文件（在 archive 目录内操作，保留最近30天）===
        echo "正在清理30天前的历史文件..."
        find ./archive -maxdepth 1 -type d -name "20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]" -mtime +30 2>/dev/null | while read old_dir; do
          echo "删除历史归档目录: $old_dir"
          rm -rf "$old_dir"
        done
        
        # ========== 创建/更新固定链接目录 ==========
        echo "更新固定链接目录 (latest)..."
        mkdir -p "./latest/full"
        mkdir -p "./latest/lite"
        # 将今日文件复制到 latest 目录，覆盖旧版本
        cp "./archive/$TODAY/full/merged_all.txt" "./latest/full/"
        cp "./archive/$TODAY/lite/merged_lite.txt" "./latest/lite/"
        # 同时复制统计文件
        cp "./archive/$TODAY/full/merge_stats.txt" "./latest/full/" 2>/dev/null || true
        cp "./archive/$TODAY/lite/optimization_stats.txt" "./latest/lite/" 2>/dev/null || true
        
        # --- 生成或更新索引文件 (index.md) ---
        # 重建索引文件
        echo "# 📁 AdGuard 规则仓库索引" > ./index.md
        echo "" >> ./index.md
        echo "此分支包含自动生成的 AdGuard 规则文件。" >> ./index.md
        echo "" >> ./index.md
        echo "## 🚀 快速订阅（推荐）" >> ./index.md
        echo "" >> ./index.md
        echo "如需在 AdGuard Home 中设置订阅，请使用以下**固定链接**，无需每日更改：" >> ./index.md
        echo "" >> ./index.md
        echo "- **精简版规则 (Lite)**: [\`/latest/lite/merged_lite.txt\`](./latest/lite/merged_lite.txt)" >> ./index.md
        echo "- **完整版规则 (Full)**: [\`/latest/full/merged_all.txt\`](./latest/full/merged_all.txt)" >> ./index.md
        echo "" >> ./index.md
        echo "## 📅 今日生成的文件" >> ./index.md
        echo "" >> ./index.md
        echo "| 版本 | 归档位置 | 规则数量 | 大小 | 生成时间 |" >> ./index.md
        echo "| :--- | :--- | :--- | :--- | :--- |" >> ./index.md
        
        # 获取今日文件的规则行数和大小
        FULL_COUNT=$(grep -c -v -E '^!|^$' "./archive/$TODAY/full/merged_all.txt" 2>/dev/null || echo "N/A")
        LITE_COUNT=$(grep -c -v -E '^!|^$' "./archive/$TODAY/lite/merged_lite.txt" 2>/dev/null || echo "N/A")
        FULL_SIZE=$(du -h "./archive/$TODAY/full/merged_all.txt" 2>/dev/null | cut -f1)
        LITE_SIZE=$(du -h "./archive/$TODAY/lite/merged_lite.txt" 2>/dev/null | cut -f1)
        TIMESTAMP=$(date +'%Y-%m-%d %H:%M:%S')
        
        echo "| 完整版 | [\`archive/$TODAY/full/merged_all.txt\`](./archive/$TODAY/full/merged_all.txt) | $FULL_COUNT 条 | $FULL_SIZE | $TIMESTAMP |" >> ./index.md
        echo "| 精简版 | [\`archive/$TODAY/lite/merged_lite.txt\`](./archive/$TODAY/lite/merged_lite.txt) | $LITE_COUNT 条 | $LITE_SIZE | $TIMESTAMP |" >> ./index.md
        
        echo "" >> ./index.md
        echo "## 📚 历史归档" >> ./index.md
        echo "" >> ./index.md
        echo "**存档策略**：自动保留最近30天的历史文件，存放于 \`archive/\` 目录下。更早的文件将被自动清理。" >> ./index.md
        echo "" >> ./index.md
        echo "> 提示：点击日期可展开查看该日生成的所有文件。" >> ./index.md
        echo "" >> ./index.md
        
        # 按日期倒序列出所有历史日期目录（清理后最多30个）
        for DATE_DIR in $(ls -d ./archive/20*/ 2>/dev/null | sort -r); do
          DATE_DIR=${DATE_DIR#./archive/} # 移除前面的 ./archive/
          DATE_DIR=${DATE_DIR%/} # 移除结尾的斜杠
          if [ "$DATE_DIR" != "$TODAY" ]; then
            echo "<details>" >> ./index.md
            echo "<summary><b>$DATE_DIR</b></summary>" >> ./index.md
            echo "" >> ./index.md
            echo "| 版本 | 文件 | 大小 |" >> ./index.md
            echo "| :--- | :--- | :--- |" >> ./index.md
            if [ -f "./archive/$DATE_DIR/full/merged_all.txt" ]; then
              SIZE=$(du -h "./archive/$DATE_DIR/full/merged_all.txt" 2>/dev/null | cut -f1)
              echo "| 完整版 | [$DATE_DIR/full/merged_all.txt](./archive/$DATE_DIR/full/merged_all.txt) | $SIZE |" >> ./index.md
            fi
            if [ -f "./archive/$DATE_DIR/lite/merged_lite.txt" ]; then
              SIZE=$(du -h "./archive/$DATE_DIR/lite/merged_lite.txt" 2>/dev/null | cut -f1)
              echo "| 精简版 | [$DATE_DIR/lite/merged_lite.txt](./archive/$DATE_DIR/lite/merged_lite.txt) | $SIZE |" >> ./index.md
            fi
            echo "" >> ./index.md
            echo "</details>" >> ./index.md
            echo "" >> ./index.md
          fi
        done
        
        echo "文件整理、索引及历史清理完成。"

    # 5. 提交并推送所有更改到 rules 分支
    - name: Commit and Push All to rules branch
      run: |
        cd ./rules-output
        
        # 配置Git用户
        git config --global user.name "github-actions[bot]"
        git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
        
        # 添加所有文件
        git add .
        
        # 检查是否有变更
        if git diff --staged --quiet; then
          echo "没有新的规则文件需要提交。"
        else
          # 提交并推送
          git commit -m "📦 Auto-update rules $(date +'%Y-%m-%d %H:%M:%S') [保留30天历史]"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:rules
          echo "✅ 所有规则文件已成功推送至 'rules' 分支。"
          echo "📄 索引页面: https://github.com/${{ github.repository }}/blob/rules/index.md"
        fi